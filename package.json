{
  "name": "@crapthings/react-wormhole",
  "description": "This hoc open a tunnel between react component.",
  "keywords": [],
  "version": "0.0.1",
  "author": "crapthings",
  "license": "MIT",
  "private": false,
  "main": "dist/index.js",
  "scripts": {
    "build": "parcel build src/index.js",
    "dev": "rm -rf dist && npm run build && parcel demo/index.html",
    "publish": "rm -rf dist && npm run build && npm publish --access public"
  },
  "peerDependencies": {
    "react": "^16.8.6"
  },
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/plugin-proposal-class-properties": "^7.5.5",
    "@babel/plugin-proposal-decorators": "^7.4.4",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  }
}
